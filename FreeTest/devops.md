# **게임 기획서: Project DevOps (가제)**

## **1\. 개요 (Overview)**

* **장르**: 인프라 매니지먼트 시뮬레이션 / 전략 / 타이쿤  
* **플랫폼**: PC (Steam, Web)  
* **타겟 유저**: 개발자, 시스템 관리자, 혹은 복잡한 시스템 최적화를 즐기는 게이머  
* **핵심 경험**: 클라우드 인프라를 구축하고, 쏟아지는 트래픽을 처리하며, 제한된 자원 안에서 시스템 안정성을 유지하는 'DevOps 엔지니어'의 경험.

## **2\. 게임 목표 (Goal)**

유저는 스타트업의 DevOps 엔지니어가 되어 각 스테이지(서비스 성장 단계)마다 주어지는 시나리오를 클리어해야 한다.

* **승리 조건**: 시나리오 시간 종료 시까지 에러율(Error Rate)을 임계치 이하로 유지하며 생존.  
* **패배 조건**: 인프라 처리 한계 초과로 인한 에러율이 일정 시간 이상 지속 (Service Down).  
* **스코어링**: 스테이지 종료 시점의 \*\*최대 처리 RPS (Requests Per Second)\*\*와 **활성 유저(DAU)** 수를 기반으로 점수 산정.

## **3\. 핵심 구성 요소 (Core Components)**

### **3.1. 유저 (Player: DevOps Team)**

* **역할**: 인프라 아키텍처 설계, 서버 증설(Scale-out/Scale-up), 인프라 연결, 장애 대응.  
* **자원**: 예산(Budget) \- 서버 유지비 및 증설 비용.

### **3.2. 요청 (Request) & 트래픽**

고객이 발생시키는 트래픽은 단순한 숫자가 아니라, 색깔이나 아이콘으로 구분되는 **속성**을 가짐.

* **Request 구조**: 하나의 Request는 여러 개의 Sub-item으로 쪼개져 처리됨.  
  * 🟢 **Static Request**: 단순 페이지 로딩 (Web Server/CDN 필요)  
  * 🔵 **DB Read**: 데이터 조회 (Read Replica, Cache 필요)  
  * 🔴 **DB Write**: 데이터 저장 (Master DB 필요, 처리 속도 느림)  
  * 🟡 **Compute**: 복잡한 연산 (High CPU Instance 혹은 Serverless 필요)  
* **처리 흐름**: Request 진입 \-\> Load Balancer \-\> Web Server \-\> (필요시) WAS/DB \-\> 응답 반환

### **3.3. 부서 간 상호작용 (Events)**

| 부서 | 역할 및 게임 내 기능 | 이벤트 예시 |
| :---- | :---- | :---- |
| **PM팀** | **\[퀘스트 발주\]** 새로운 기능 요구, 시나리오 난이도 조절. | "채팅 기능 추가해주세요." (Socket 통신 인프라 필요) "검색 기능 강화합니다." (DB Read 부하 증가) |
| **마케팅팀** | **\[트래픽 공격\]** 예고된/불시의 트래픽 급증 유발. | "내일 TV 광고 나갑니다\!" (내일 트래픽 500% 증가 예고) "SNS에서 바이럴 터졌어요\!" (갑작스런 트래픽 스파이크) |
| **개발팀** | **\[기술 해금 & 디버프\]** 새로운 아키텍처 도입, 코드 배포. | "MSA 도입 준비 완료." (컨테이너 인프라 해금) "배포했는데 메모리 누수가 있네요." (특정 서버 효율 30% 감소) |

## **4\. 인프라 시스템 (Infrastructure)**

인프라는 노드(Node) 형태로 맵에 배치하며, 각 노드는 용량(Capacity)과 특성이 다름.

### **4.1. 인프라 타입 (Tier)**

1. **Compute**  
   * VM (Basic): 범용, 저렴, 처리량 낮음.  
   * VM (High-CPU): 연산 처리에 특화.  
   * Serverless Function: 트래픽에 따라 자동 스케일링되나 요청당 비용이 비쌈 (Cold start 페널티 존재).  
   * Container (K8s): MSA 도입 후 사용 가능. 유연한 관리.  
2. **Database**  
   * SQL (Master): Write/Read 모두 가능. 병목의 중심.  
   * SQL (Slave/Read Replica): Read만 처리 가능. Master 부하 분산.  
   * NoSQL: 특정 데이터 구조 처리에 빠름.  
   * Cache (Redis): 반복되는 Read 요청을 즉시 처리.  
3. **Network & Etc**  
   * Load Balancer: 트래픽을 여러 VM으로 분산. 필수 요소.  
   * Queue (Kafka/RabbitMQ): 폭주하는 요청을 임시 저장하여 에러 방지 (지연 처리).

### **4.2. 연결 및 병목**

* 유저는 각 노드를 선(Line)으로 연결해야 함.  
* 단일 노드에 너무 많은 연결이 몰리면 해당 노드가 병목(Bottleneck)이 되어 전체 시스템 속도 저하.

## **5\. 스테이지 진행 (Game Loop)**

### **Phase 1: 준비 (Setup)**

* 기본 자금과 초기 아키텍처(Monolithic) 제공.  
* 초기 예상 트래픽 그래프 확인.

### **Phase 2: 운영 (Operation) \- 실시간 진행**

1. **트래픽 유입**: 시간이 흐르며 고객 수 증가.  
2. **이벤트 발생**:  
   * *PM*: "신규 가입 이벤트 시작합니다." \-\> 회원가입 로직(DB Write) 비중 증가.  
   * *마케팅*: "푸시 알림 발송\!" \-\> 1분간 접속자 폭주.  
3. **유저 대응 (Action)**:  
   * **Scale-up**: 기존 서버를 더 비싼 고성능 서버로 교체.  
   * **Scale-out**: 동일한 서버를 여러 대 복제하여 로드 밸런서에 연결.  
   * **Architecture Change**: 개발팀에 요청하여 MSA 전환 (비용 및 시간 소요).  
4. **피드백**:  
   * 처리량 초과 시 화면이 붉게 점멸하며 502 Bad Gateway 에러 카운트 상승.  
   * 에러 게이지가 꽉 차면 **Service Down (Game Over)**.

### **Phase 3: 평가 (Result)**

* 생존 여부 확인.  
* 최대 트래픽(Max TPS), 평균 응답 속도(Latency), 가동 시간(Uptime)을 종합하여 랭크(S\~F) 부여.

## **6\. UI/UX 컨셉**

* **메인 화면 (Dashboard View)**:  
  * 중앙: 서버 토폴로지 맵 (노드와 연결선). 트래픽이 흐르는 것이 데이터 패킷 애니메이션으로 보임.  
  * 상단: 실시간 트래픽 그래프 (성공/에러), 현재 접속자 수.  
  * 좌측: 로그 창 (System Log, 부서별 메시지 수신).  
  * 하단: 인프라 건설 메뉴 (Shop).  
* **시각적 피드백**:  
  * 과부하 걸린 서버는 빨갛게 달아오르거나 연기가 남.  
  * 데이터베이스 Lock이 걸리면 자물쇠 아이콘 표시.

## **7\. 시나리오 예시**

### **Stage 1: "Hello World" (튜토리얼)**

* **목표**: 첫 웹서비스 런칭.  
* **구성**: 단일 VM \+ 단일 DB.  
* **이벤트**: 친구들에게 링크 공유 (소규모 트래픽).  
* **학습**: VM 증설 및 로드 밸런서 개념 학습.

### **Stage 3: "바이럴 마케팅의 습격"**

* **상황**: 갑자기 SNS에서 서비스가 화제가 됨.  
* **특징**: Read 요청이 기하급수적으로 증가.  
* **공략**: DB Read Replica 도입 및 캐시 서버(Redis) 설치가 필수.

### **Stage 5: "블랙 프라이데이"**

* **상황**: 1년 중 가장 큰 세일 행사.  
* **특징**: 엄청난 양의 트래픽과 결제 요청(Write) 폭주. 개발팀이 MSA를 제안함.  
* **공략**: 서비스를 마이크로서비스로 분리하고, 대기열(Queue) 시스템을 도입하여 DB 터짐을 방지해야 함.